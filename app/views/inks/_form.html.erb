<%= form_for(ink) do |f| %>
  <% if ink.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ink.errors.count, "error") %> prohibited this ink from being saved:</h2>

      <ul>
      <% ink.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :model %>
    <%= f.text_field :model %>
  </div>

  <div class="field">
    <%= f.label :manufacturer %>
    <%= f.text_field :manufacturer %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
