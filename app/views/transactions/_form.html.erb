<%= form_for(transaction) do |f| %>
  <% if transaction.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(transaction.errors.count, "error") %> prohibited this transaction from being saved:</h2>

      <ul>
      <% transaction.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.text_field :user_id %>
  </div>

  <div class="field">
    <%= f.label :equipment_id %>
    <%= f.text_field :equipment_id %>
  </div>

  <div class="field">
    <%= f.label :RentedDatetime %>
    <%= f.datetime_select :RentedDatetime %>
  </div>

  <div class="field">
    <%= f.label :ReturnedDatetime %>
    <%= f.datetime_select :ReturnedDatetime %>
  </div>

  <div class="field">
    <%= f.label :contract %>
    <%= f.text_field :contract %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
